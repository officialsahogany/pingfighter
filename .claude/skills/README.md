# Skill Tree Policy

`.claude/skills/` is the canonical repo skill tree for 환격전.

If `.agents/skills/` exists, treat it as a Codex loader mirror only. Do not
edit both trees by hand. Update this canonical tree first, then refresh the
mirror from here only when the loader requires it.
