# 동네게임즈 로고 텍스트 연출 구현 프롬프트

현재 `logo_intro.py`는 검은색 배경에 따뜻한 크림톤 글로우(Radial Bloom)와 부드러운 스케일 인(Scale-in) 연출이 적용되어 있어 매우 세련된 상태입니다. 여기에 **"동네게임즈"**라는 텍스트를 더해 완성된 게임 스튜디오의 인트로 느낌을 주려고 합니다.

아래 4가지 연출 아이디어 중 하나(또는 조합)를 선택하여 `logo_intro.py`에 구현해 주세요.

## 💡 연출 아이디어 4선

1. **레트로 타이프라이터 (Typewriter) 💻 - 픽셀/레트로 감성 극대화**
   * **연출:** 로고가 `Scale-in` 하며 화면에 안착하는 순간 (약 300ms 이후), 로고 아래에 텍스트가 **`동 . 네 . 게 . 임 . 즈 _`** 한 글자씩 타닥타닥 타이핑되며 나타납니다.
   * **디테일:** 끝에 깜빡이는 커서(`_` 또는 `█`)를 추가하여 옛날 오락실 기동 화면이나 도스(DOS) 시절의 부팅 감성을 살립니다.

2. **시네마틱 페이드인 & 자간 수축 (Tracking / Letter-spacing) 🎬 - 고급스러움**
   * **연출:** 로고 등장 후, "동네게임즈"라는 글자가 로고 아래에서 **아주 넓은 자간(`동   네   게   임   즈`)으로 시작해서 점점 원래 자간으로 좁혀지며** 부드럽게 페이드인(Fade-in) 됩니다.
   * **디테일:** 유명 영화사나 대형 게임 스튜디오 인트로처럼 묵직하고 웅장한 느낌을 줍니다. 텍스트 색상은 로고의 글로우(크림톤) 색상을 은은하게 띕니다.

3. **네온사인 깜빡임 (Neon Flicker) ⚡ - 오락실/아케이드 감성**
   * **연출:** 로고 등장 후, 텍스트가 아케이드 네온사인처럼 **파직! 하고 한두 번 깜빡이다가** 스르륵 켜집니다.
   * **디테일:** "동네"라는 친근하고 골목길스러운 키워드와 네온사인 특유의 켜지는 연출이 만나 감성적인 시너지를 냅니다.

4. **빛이 퍼지는 실루엣 등장 (Glow Reveal) 🌅 - 일체감 있는 연출**
   * **연출:** 텍스트가 처음부터 보이지 않고, 로고 뒤에서 뿜어져 나오는 **크림톤의 둥근 글로우(Glow) 빛이 아래로 살짝 퍼지면서** 어둠 속에 숨어있던 "동네게임즈" 글자를 스윽 비춰내는 연출입니다.
   * **디테일:** 텍스트 자체가 페이드인 되는 게 아니라, "빛이 닿아서" 글씨가 드러나는 형태라 로고 연출과 완벽한 일체감을 줍니다.

## 🛠️ 구현 시 주의사항 (제약 조건)

* **폰트:** 프로젝트에 설정된 레트로 픽셀 폰트(`font_config.py`의 `PF스타더스트` 등)를 활용해 주세요.
* **성능:** 현재 `logo_intro.py`의 핵심인 **사전 렌더링(캐싱) 및 매 프레임 할당 최소화** 기조를 유지해야 합니다. 텍스트 렌더링(`font.render`)을 매 프레임 호출하지 말고, 필요한 텍스트 Surface(들)를 `_build_logo_intro_cache` 단계에서 미리 만들어두거나 초기화 시점에 한 번만 생성하여 사용해 주세요.
* **타이밍:** 로고의 `Scale-in`(초기 300ms)이 끝날 즈음이나 직후에 텍스트 연출이 시작되도록 타이밍을 맞춰주세요.
* **해상도 대응:** 화면 크기(`screen_size`)에 비례하여 텍스트 크기와 배치 위치(로고 아래 적절한 여백)가 동적으로 계산되도록 해주세요.
