# -*- coding: utf-8 -*-
"""
게임 상수 정의 모듈
모든 게임 상수를 체계적으로 관리
"""

# ============================================================
# 색상 상수
# ============================================================
class Colors:
    """게임 전체에서 사용되는 색상 상수"""
    # 기본 색상
    WHITE = (255, 255, 255)
    BLACK = (0, 0, 0)
    RED = (255, 0, 0)
    GREEN = (0, 255, 0)
    BLUE = (0, 0, 255)
    YELLOW = (255, 255, 0)
    CYAN = (0, 255, 255)
    MAGENTA = (255, 0, 255)
    
    # 회색 계열
    GRAY = (128, 128, 128)
    LIGHT_GRAY = (192, 192, 192)
    DARK_GRAY = (64, 64, 64)
    
    # 스테이지별 테마 색상
    STAGE_COLORS = {
        1: (0, 150, 100),  # 사이버펑크 청록
        2: (0, 100, 200),  # 심해 파랑
        3: (255, 0, 128),  # 네온 핑크
        4: (255, 215, 0),  # 황금
        5: (255, 100, 0),  # 용암 주황
        6: (128, 0, 255),  # 보라
    }

# ============================================================
# 크기 상수
# ============================================================
class Sizes:
    """크기 관련 상수"""
    SMALL = 10
    MEDIUM = 20
    DEFAULT_RADIUS = 20
    LARGE = 50
    ICON = 32
    TILE = 40
    
    # 화면 크기 (config에서 가져옴)
    WINDOW_WIDTH = 1024
    WINDOW_HEIGHT = 768

# ============================================================
# 속도 상수
# ============================================================
class Speeds:
    """속도 관련 상수"""
    SLOW = 3
    DEFAULT = 5
    FAST = 10
    MAX_BALL = 15
    
    # 패들 속도
    PADDLE_SLOW = 3
    PADDLE_DEFAULT = 5
    PADDLE_FAST = 8

# ============================================================
# 타이밍 상수
# ============================================================
class Timings:
    """타이밍 및 프레임 관련 상수"""
    FPS = 60
    MILLISECONDS_PER_SECOND = 1000
    HALF_SECOND_FRAMES = 30
    ONE_SECOND_FRAMES = 60
    TWO_SECONDS_FRAMES = 120
    THREE_SECONDS_FRAMES = 180

# ============================================================
# UI 상수
# ============================================================
class UI:
    """UI 관련 상수"""
    DEFAULT_ALPHA = 150
    GLOW_ALPHA = 50
    BORDER_WIDTH = 4
    
    # 각도
    QUARTER_ROTATION = 90
    HALF_ROTATION = 180
    FULL_ROTATION = 360

# ============================================================
# 게임플레이 상수
# ============================================================
class Gameplay:
    """게임플레이 관련 상수"""
    # 점수
    BASE_SCORE = 10
    COMBO_MULTIPLIER = 1.5
    
    # 난이도
    EASY_SPEED_MULTIPLIER = 0.8
    NORMAL_SPEED_MULTIPLIER = 1.0
    HARD_SPEED_MULTIPLIER = 1.2
    
    # 보스 체력
    BOSS_HP_BASE = 3
    BOSS_HP_PER_STAGE = 2