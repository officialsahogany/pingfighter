# 지시문 S4 — 생성기: 1~2층 수호의 샘터 1개 보장 (병렬 소형)

- **발행**: 관제탑 2026-08-24. 기준 HEAD `63f8cb8f4`. CI/pre-push 락스텝 229.
- **격리 워크트리**: `D:\codex_tmp\bosspong_spring_s4_63f8` (브랜치
  `codex/spring-early-guarantee-20260824`). S1~S3와 파일 무겹침
  (`tower_ascent_map_generator.gd`+계약 스모크) — **병렬 가능**.
- **금지**: 본 트리 직접 편집·푸시·통합. 보고 후 대기.

## 배경

샘터 개편으로 첫 수호령 획득이 샘터 이벤트가 된다. 그런데 샘터는
`TEMP_NODE_TYPE_WEIGHTS`에서 최저 가중치(1: 상점2·수련2·휴식2·파계승1)
라 런 초반 등장이 비보장 — 128시드 실측에서 1층 서비스 분포
guardian_spring 438 vs shop 724. 대표 온보딩이 시드 운에 걸리면 안 된다.

## 작업

1. `tower_ascent_map_generator.gd`에 보장 패스 추가: 생성 후 1~2층
   비전투 노드에 guardian_spring이 0개면 해당 구간의 비전투 노드 1개를
   샘터로 치환(치환 우선순위: 같은 종류 중복이 있는 노드부터·보스/관문
   비접촉). 이미 있으면 무변경.
2. ⚠기존 계약 불변식 유지: 노드 수·간선·보스 비율(0.34)·NPC:boss≥2·
   싱글톤 인접 규칙·`standin_duplicate_gate` 의미론은 건드리지 않는다.
   치환은 비전투 kind 간 교체만.
3. ⚠GRT-054: 보장 층 범위(1~2)와 보장 개수(1)는 TEMP_ 상수로.
4. GENERATOR_VERSION 문자열 bump(현행 v12 → v13) — 시드 계약 세대 명시.

## 씰

- 128시드 전수: 모든 시드에서 1~2층 guardian_spring ≥ 1 단언 +
  기존 `tower_ascent_floor_one_expansion_contract_smoke` 전 레그 GREEN
  유지(분포 통계 변화는 수치 갱신 보고).
- 치환 발생/미발생 양쪽 레그(이미 있던 시드=무변경 단언).
- draw call 예산 재실측: 최악 경로 1536/1536 꽉 참(GRT-043) — 치환으로
  노드 아이콘 구성이 바뀌는 시드의 예산 재확인, 상한 초과 시 중단·보고.
- RED 반증: 보장 패스 무력화 시 1~2층 샘터 0개 시드 존재 확인.
- 게이트: 포커스드 스모크 → `-Paths` 경고 → 헤드리스 로드 → diff.

## 보고

워크트리·커밋 해시·씰 종단선·분포 변화 수치(치환 발생 시드 수)·예산
재실측치·미해결.
