# 지시문 Q6 — 경로 공 벽 사운드·속도 411·가속 결합 제거 (피드백5 8·8·9항)

- **발행**: 관제탑 2026-08-25. 기준 HEAD `902312d3e`. CI/pre-push 락스텝 236.
- **격리 워크트리**: `D:\codex_tmp\bosspong_routeball_9023` (브랜치
  `codex/fb5-route-ball-physics-20260825`). 주 파일=route_serve_runtime·
  튜닝·route 씰 — Q1~Q5와 무겹침, 병렬 가능.
- **금지**: 본 트리 직접 편집·푸시·통합. 보고 후 대기.

## ⑧a 벽 타격 사운드

- 실측: 경로 서브 런타임에 오디오 참조 0건. 정본=`game_audio.
  play_wall_hit(impact_speed, source_x)`(쿨다운 0.035s·피치·위치 패닝
  내장).
- 작업: `begin()`에서 `_get_instance(registry, "game_audio")` 보관
  (GRT-003·선례 tower_ascent_modal_lifecycle:26) → 단일 헬퍼로 3개
  바운스 지점(라이브 측벽 :374~·라이브 상단 :389~·픽스처 상단
  :427~, 픽스처는 오디오 null이라 자연 무음) 호출. 상단벽도 좌우와
  동일음 재사용(전투에 상단 소리 계약 없음 — 기본안).

## ⑧b 서브 속도 +50% (274 → 411)

- `TEMP_ROUTE_AIM_SERVE_SPEED_PER_SECOND` 274.0 → **411.0** 한 줄 —
  전 소비자(런타임 2곳·씰 파생 5곳)가 상수 파생 확인 완료라 코드
  추가 수정 불요.
- ⚠이력 명기: 522→365.4("여전히 빠름")→274 두 차례 감속 이력이
  있고 411은 기각된 365.4보다 빠름 — **이번 +50%는 사용자 명시
  지시**(가속 결합 제거·감속 없는 순수 반사와 세트라 체감이 다름).
  튜닝 주석의 감속 이력에 이번 결정 1줄 추가.
- 동반 갱신: 바람 드리프트 수치표 주석(64/129/193px → 재계산치
  ≈28/57/86px), 0.009 채택 근거 주석 재선언.

## ⑨ "벽 가속" 제거 (실측: 벽 반사는 무배율 — 진범 2건)

1. **[1순위] 패들 바운스의 전투 결합 제거**: 경로 공이 플레이어
   패들에 맞으면 `get_minimum_rally_speed()`(전투 스케일 ≈633px/s)
   하한 클램프로 순간 2.3배 가속 + 타격당 랜덤 배율·엣지 1.2배.
   수리: 경로 리졸버 호출의 minimum을 서브 속도(TEMP/60) 기반으로,
   accel_scale 1.0, 나가는 속력을 들어온 속력으로 캡.
2. **[2순위] 바운스 시점 속력 재정규화**: 바람 x가속 누적이 벽 접촉
   순간 최대(세기3 +23%p)라 "벽이 가속"으로 체감 — 벽/패들 바운스
   순간에만 속력 크기를 서브 속도로 재정규화(방향 유지). ⚠비행 중
   전체 재정규화 금지: "바람은 x만·y 불변" 씰(route_serve_smoke
   :745~·wind_target_smoke :183~)을 깨면 안 됨 — 바운스 인스턴트
   한정임을 씰로 증명.

## 씰·재실측 (관제탑 프로브 목록)

- 신규 SFX 씰: fake game_audio 주입 라이브형 픽스처 — 측벽·상단
  바운스 각 1회 play_wall_hit 호출 단언 + 무바운스 직선 비행 0건
  음성 레그. ⚠측벽 레그는 라이브 경로(_advance_live_ball) 관통 필수
  (픽스처 비행엔 측벽 반사 없음).
- 패들 가속 제거 씰: 바운스 후 속력 == 들어온 속력 단언 + 전투
  min-rally 클램프 복원 RED 반증(현행 4.57→10.55 점프가 관측 실패).
- 바운스 재정규화 씰: 강풍 누적 후 벽 접촉 → 속력 재정규화·방향
  유지 + x만/y 불변 기존 레그 GREEN 유지.
- **411 재실측 필수**: `tower_ascent_route_serve_smoke` 전체 — 특히
  바람 실질성 역반증 레그(:835~, 마진 130→23px 급감) 실측.
  자동 파생 형제 4종(wind_target·wind_and_pickup·snapshot_recovery·
  vertical_slice) 재실행 — RED면 파생 누락 발견 보고.
- 게이트: 포커스드 스모크(+RED 반증) → `-Paths` 경고 → 헤드리스
  로드 → diff → 라이브 지도 1판(벽음 청취·411 조준 체감) 보고.

## 보고

워크트리·커밋 해시·씰 종단선 원문·재실측 결과(실질성 마진 실측치
포함)·라이브 확인·미해결.
