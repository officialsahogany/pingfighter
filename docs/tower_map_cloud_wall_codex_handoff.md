# 지시문 J3 — 지도 잠금 구역 "먹구름 벽" 전환 (층 밴드 분리 폐지)

- **발행**: 관제탑 2026-08-24. 기준 HEAD `e75728ba8`. CI/pre-push 락스텝 230.
- **격리 워크트리**: `D:\codex_tmp\bosspong_cloudwall_e757` (브랜치
  `codex/tower-map-cloud-wall-20260824`). 주 파일
  `tower_ascent_map_cloud_layer.gd`+구름 계약 씰 — 병렬 안전.
- **금지**: 본 트리 직접 편집·푸시·통합. 단계 보고 후 대기.

## 사용자 확정 룩 (레퍼런스 필수 참조)

정본 레퍼런스: `docs/reference_art/tower_map_cloud_wall_reference_20260824.png`
(사용자 제공). 요구사항:

1. 잠긴 구역 전체가 **분리 없는 하나의 큰 구름 덩어리** — 현행 층별
   밴드(겹겹이 쌓인 흰 띠 느낌) 폐지.
2. **흰색/크림 평면 띠 제거** — 속이 채워진 자욱한 먹구름 질감으로.
   레퍼런스처럼 소용돌이 먹운(짙음)과 밝은 운무(옅음)가 뒤섞인 벽.
3. 하단 경계는 공개된 지도로 유기적으로 디졸브(길이 구름 속으로
   사라지는 인상). 직선/반복 패턴 티가 나면 안 됨.
4. 차폐 기능 유지: 잠긴 층의 노드·간선은 판독 불가(기존 ≥0.9 계약).
5. 층 공개(리빌) 시 구름 벽의 하단 경계가 한 층만큼 위로 걷힘 —
   기존 리빌 애니메이션 시간원(_map_ambient_drift 합류 계약) 유지.

## A단계 — 아트 (승인 게이트)

1. 자산 후보 생성(이미지 스킬 경유, 기존
   `assets/sprites/tower/map_scroll/cloud_*_imagegen_v1` 파이프라인):
   - **먹구름 벽 대형 타일** 1~2종: 가로 무한 타일 가능(seamless-x),
     세로로도 이어붙일 수 있는 내부용 1종 + 상부용 1종(선택). 알파
     불투명 코어(차폐 담당) + 레퍼런스 팔레트(양피지 배경과 동계열
     먹/회갈, 순백 금지 — "흰 띠" 인상 재발 방지).
   - **하단 디졸브 스트립** 1종: 위=불투명 구름, 아래=완전 투명으로
     녹는 seamless-x 전이대. 소용돌이 실루엣 가장자리.
2. 후보 검수 시트(레퍼런스와 나란히)와 함께 **관제탑 승인 보고 후
   대기**. 기존 구름 모티프 4종은 벽 위 상시 드리프트 악센트로 존치
   가능(재생성 불필요).

## B단계 — 런타임 (A 승인 후)

1. `tower_ascent_map_cloud_layer.gd` 재구성: 층별 fog 스펙을 **연속
   잠금 구간 병합 영역** 1개로 통합 — 영역 = [공개층+1 .. 최상층]의
   Y범위 전체(층간 갭 포함, 띠 분리 원천 제거). 내부=벽 타일 채움,
   하단=디졸브 스트립, 모티프 드리프트는 그 위.
2. 리빌: 경계가 층 단위로 상승. 진행 중에는 걷히는 층 구간의 알파
   멀티플라이어로 페이드(기존 per-floor reveal 시간원 소비 유지 —
   피드백2 5항 계약). 공개 완료 층 위에는 픽셀 0.
3. ⚠GRT-043: 현행 예산 층당 13콜×층수 → 병합 설계는 콜이 크게 줄
   여지가 있음. 재배치 시 `BITMAP_MAX_DRAW_CALLS_PER_FLOOR`·reserve·
   128시드 최악 예산(현 1536/1536)·`floor_one_expansion_contract` 예산
   레그를 한 커밋에서 락스텝 갱신.
4. ⚠GRT-004: 신규 텍스처는 자산 착지+file_exists/사이즈 계약
   (`tower_map_scroll_asset_catalog.gd`) 등재와 같은 슬라이스. 프리웜
   경유(GRT-042), VRAM 압축 임포트.
5. ⚠GRT-047: 어두운 먹 텍스처의 블렌드 재검 — 차폐 코어는 MIX 불투명,
   ADD 금지. GRT-056: canvas.material 스왑 금지.
6. J2의 물결 페더 코드는 이 설계로 대체됨 — 잔재 정리(씰 개정 동반).

## 씰

- 구름 계약 씰 개정: 층별 1fog+1haze+4motif 구성 계약 → 병합 영역
  계약(잠금 구간 1영역·내부 차폐 ≥0.9·경계 스트립 존재·모티프 수)으로
  재정의. 콜 예산 레그 락스텝.
- 리빌 레그: 층 공개 전/중/후 3단 — 공개층 위 픽셀 0·진행 중 페이드·
  잠긴 내부 판독 불가 유지. RED 반증(병합 무력화 시 띠 분리 재현 또는
  차폐 붕괴).
- 픽셀 QA(비헤드리스 Vulkan, 줌 3단): ①층간 경계 위치에서 수평 스캔
  — 인접 행 델타 급락(띠 경계) 부재, ②하단 디졸브 구간 단조 전이,
  ③잠긴 노드 은닉. 레퍼런스 나란히 검수 시트.
- 게이트: 포커스드 스모크(+RED 반증) → `-Paths` 경고 → 헤드리스 로드
  → `git diff --check` → 캡처(줌 3단+리빌 중 1장).

## 보고

단계별: A=후보 시트·프롬프트·거부 후보 경로 / B=커밋 해시·씰 종단선
원문·예산 재실측·캡처 경로·미해결.
