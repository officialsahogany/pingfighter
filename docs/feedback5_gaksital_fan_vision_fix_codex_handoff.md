# 지시문 Q4-수정 — 입력 충돌 P0 + 아이콘/퍼프 (관제탑 검토 회신)

- **발행**: 관제탑 2026-08-25. 대상: 브랜치
  `codex/fb5-gaksital-fan-vision-20260825`(워크트리
  `D:\codex_tmp\bosspong_fanvision_4755`)의 `e962942c3` 위 **추가
  커밋**. amend/rebase 금지. 본 트리 편집·푸시·통합 금지.
- **판정**: 공유 계약 추출·획득 배선·보스 CC·카탈로그 체인·GRT-060
  확장은 APPROVE — 재작업 금지. 아래만 수리.

## [P0] 입력 충돌 — Shift+오른쪽은 빈 에지가 아니다

- 실측: 청린귀 비전이 `vision_modifier` 아래 **좌우 에지를 모두**
  커맨드(Left→Right→Left)로 소비 중. 부채가 인덱스 1로 먼저 처리돼
  중간 Right에서 발동→게이지 −80이 공유 config에 즉시 반영되고,
  청린귀 게이트(250)가 170을 보고 커맨드를 리셋 —
  **지맥진동이 게이지 330 미만에서 사실상 봉인**되고 부채는 시도마다
  기생 발동. 1층 각시탈→2층 청린귀 정규 진행에서 재현.
  ⚠제 지시문의 "빈 방향 에지" 전제가 틀렸다 — 사과와 함께 정정.
- **사용자 확정 입력: `vision_modifier` 홀드 + 마우스 우클릭.**
  방향 에지를 전혀 쓰지 않아 청린귀·달지·연묘와 원천 무충돌.
  - 툴팁/HUD/도움말 문구를 전부 새 입력으로 갱신(7개 국어).
  - ⚠기존 우클릭 소비자와의 충돌을 먼저 전수 확인(전투 중 우클릭
    바인딩·모달/툴팁 우클릭 등). 충돌이 있으면 **중단하고 보고**.
  - `vision_modifier` 홀드 중 수평 입력 잠금 계약은 그대로 유지.
- **씰 필수**: 청린귀 비전과 동시 보유 상태에서 (a) 우클릭 발동이
  청린귀 커맨드 상태를 건드리지 않음, (b) 청린귀 좌우 커맨드가 부채를
  발동시키지 않음, (c) 두 비전 동시 보유 시 게이지 잠금 부재 —
  현행 코드가 RED임을 먼저 확인(반증).

## [P1] 동반 수리

1. **퍼프 서수 중복**: `battle_playfield_scene_drawer.gd:290`이
   `"17c.gaksital_vision_chosik"`로 청린귀(`:294`)와 같은 서수 —
   `17d`로 분리(BattlePerf 정렬 오염).
2. **아이콘 자산 미승격**: 오브 아이콘·비급 아이콘이 보스 투사체 원본
   PNG를 직접 가리킴. 다른 비전 3종은 전용
   `*_skill_orb_imagegen_v1.png`·`*_manual_icon_imagegen_v1.png`를
   갖는다. **관제탑 아트 발주 대기 항목**이므로 이번 커밋에서는
   placeholder임을 코드 주석과 보고에 명시만 하고 자산 생성은 금지.

## [P2] 선택 정리

- 공유 계약의 `projectile.duplicate(true)`(프레임당·부채당)와
  렌더러의 매 드로우 Dictionary 신규 할당 — 인플레이스/캐시로.
- 신규 비전 쿨다운 레그에 RED 반증 픽스처 추가(보스 레그 전례),
  `state.set` 주입 대신 실제 발동 경유.
- `circle_overlaps_rect` 6번째 사본 → 기존 공용 static 재사용.
- 신규 `.gd` 4종의 `.uid` 동반.
- **보고 누락 공시**: `stage1_gaksital_fan_throw_smoke`는 기저 RED
  였고(오디오 소유권 이관으로 `FAN_SOUND_PATH` 부재) 이 커밋이 동반
  수리했다 — 보고에 명시.

## 게이트·보고

기존 씰 + 신규 충돌 씰(+RED 반증) → `-Paths` 경고 → 헤드리스 로드 →
`git diff --check` → 발동 캡처 재촬영(새 입력 표기 포함). ⚠형제 비전
스모크(달지·청린귀)는 격리 워크트리 임포트 캐시 부족으로 RED이니
**기저 대조로 귀속 분리**해 보고. 보고=추가 커밋 해시·씰 종단선·
우클릭 충돌 감사 결과·캡처 경로·미해결.
