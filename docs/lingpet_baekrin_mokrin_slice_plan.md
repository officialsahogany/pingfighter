# 신규 수호령 백린 / 묵린 — 슬라이스 계획

작성 2026-07-30. 상태: **계획 (코드 미착수)**. 정본 결정은 §1, 미결은 §5.

관련: `docs/lingpet_guardian_duration_redesign_plan.md`(지속시간 풀),
`docs/sprite_socket_composition_contract.md`(M+N),
`docs/godot_runtime_traps.md`(parked!=disabled / idle-게이트 / owner 스키마),
커밋 `07518d1b1` + `9e0e7e4ec`(온이마루 탑승 회귀 4건 선행 수정).

---

## 1. 확정된 결정 (사용자)

| # | 결정 | 근거 |
|---|---|---|
| D1 | **안장은 액티브 스킬이다. 해금되어야 탑승 가능.** 발동은 쿨다운 자동이 아니라 해금 후 상시 유저 토글 | 사용자 확정 2026-07-30 |
| D2 | **현행 온이마루 탑승은 미완성**이다 — 게이트 없이 탑승된다. `오니마루의안장`을 추후 제작해 같은 게이트에 올린다 | 사용자 확정 2026-07-30 |
| D3 | **탑승 구도 = 탑다운** (제공 에셋 방향) | 사용자 확정 2026-07-30 |
| D4 | 탑승 중 전용 스킬 2종은 캐릭터 5-오브 kit을 **대체**한다 (뿔딸기 / 오딘의혼 방식) | 기획 원문 "기존 스킬이 사라지고 … 마치 뿔딸기나 오딘의혼처럼" |
| D5 | **판정 지표 = 포인트당 승률 상승폭** (`net/min` 금지) | 사용자 확정 2026-07-31. Δnet/min의 82~100%가 "이겨서"가 아니라 "포인트를 덜 치러서"라 net/min은 랠리 연장에 과잉 보상 |
| D6 | **5-1 = P0.** 목린 가드에 신규 공속 보너스 **없음**. 캡 밖 자원(게이지 회복)도 **없음** | 사용자 확정. 자동 가드 자체가 이미 핵심 효용이라 추가 보상 근거가 없음 |
| D7 | **목린변신 쿨다운 = 40초** | 사용자 확정 |
| D8 | **비교 가족 = 유령소환 계열**(40초) | 사용자 확정 |
| D9 | **조작 박탈 상한 = 소환 활성시간의 12.5%** | 사용자 확정. `cd = 5 / 0.125 = 40`으로 D7과 정합 |
| D10 | **5-1 변형은 첫 슬라이스에서 연출 전용** (가드 카운트에 따른 잔상/틴트/SFX/오라 단계) | 사용자 확정. 상태에 남는 수치 주장이 없어 툴팁 거짓 위험 0 |
| D11 | **P3(캡 해제) 후보에서 제외** ⇒ **보스 대쉬 통합 재실행 생략** | 사용자 확정. §대쉬 경계 재실행 프로토콜은 참조용으로만 보존 |
| D12 | **플레이어 표기 = `묵린`** (墨, 아트와 일치 + 백린 대칭). **내부 ID는 `mokrin` 유지**(호환성) | 사용자 확정 2026-07-31. 기획 원문의 `목린`(木)은 아트와 어긋남 |
| D13 | **첫 슬라이스 변신 연출 = 정상 12프레임만 사용**(f1-6 백색 홀드 + f7-12 모프). **RGB 틴트는 필수 조건에서 제외** | 사용자 확정. 18프레임 원본은 f13-18에서 꼬리가 소실되고(§3), 컴패니언 렌더러에 **검증된 RGB 틴트 경로가 없다**(`modulate = Color(1,1,1,alpha)`) |

D4의 결과로 **파워 예산 평가가 뒤집힌다**: 탑승은 4~5개 kit을 2개로 교환하는
것이므로, 전용 스킬이 충분히 강하지 않으면 탑승 자체가 순수 손해다. 이는 §4의
슬라이스 순서(전용 스킬 없는 탑승 선행 랜딩 금지)를 강화한다.

---

## 2. D1이 만드는 마찰점과 해소 방법 (전부 코드 확인)

### 2-1. 온이마루 유예 (D2의 직접 결과)

현재 게이트는 펫 id 화이트리스트 하나뿐이다 —
`lingpet_mount_state.gd:31` `MOUNT_SUPPORTED_PET_IDS := ["onimaru"]`. **장착 스킬을
조회하지 않는다**(보유 여부도, 슬롯 장착 여부도). 그래서 게이트를 스킬 기반으로 일반화하면 **온이마루는
`오니마루의안장`이 없어 탑승 기능을 잃는다**(현행 동작 회귀).

권고: 게이트를 `펫 id -> 안장 스킬 id` 맵으로 만들고, 스킬이 아직 없는 펫은
**명시적 유예 목록**에 두어 현행 동작을 유지한다. 유예는 코드에 이유와 만료
조건(해당 안장 스킬 랜딩)을 적고 씰로 봉인한다 — 조용한 예외는 금지.

```
MOUNT_SADDLE_SKILL_IDS := { "baekrin": "baekrin_saddle", "onimaru": "onimaru_saddle" }
MOUNT_UNGATED_LEGACY_PET_IDS := ["onimaru"]   # 오니마루의안장 랜딩 시 제거
```

### 2-2. 트리거 라벨이 "자동"으로 하드코딩되어 있다

`scripts/stages/common/lingpet_rail_card.gd:226-227`이 링펫 카드마다
`"trigger_type": "auto"` / `"trigger_label": "자동"`을 박아 넣는다(확인). 토글
스킬을 그대로 올리면 HUD가 **거짓 표기**를 한다. 카드 빌더에서 trigger를
스킬별로 파라미터화하고 `"토글"` 라벨을 추가한다. 보스 카드 쪽에는 이미
`trigger_type`을 인자로 받는 선례가 있다(`stage1_dalji_boss_skill_cooldown_state.gd:191`).

### 2-3. 공유 쿨다운 10초가 자유 토글과 충돌한다

`lingpet_egg_runtime.gd:131` `COMPANION_SKILL_SHARED_COOLDOWN_SECONDS := 10.0`,
발동 시 `start_shared_cooldown(...)`(:3155). 안장이 이 쿨다운을 소모하면
"자유롭게 타고 내림"이 성립하지 않고, 목린변신의 쿨다운까지 잡아먹는다.

그런데 **"시작하지 않음"만으로는 부족하다.** `_apply_shared_cooldown_to_current`
(`lingpet_companion_skill_persistence.gd:252`)가 `state.cooldown = maxf(state.cooldown,
shared_cooldown)`을 **모든 슬롯 상태에 역으로 전파**한다(확인). 즉 목린변신을 쓰면
안장 카드도 10초 쿨다운 상태가 된다.

permit 계약 (S2에 전부 포함):

- 공유 쿨다운을 **시작하지도, 전달받지도** 않는다
- 자동 무장 / trigger count / launch feedback에서 제외
- 스냅샷에 **`activation_model`** 과 **`interaction_active` / `interaction_available`** 투영
- 레일은 **라벨뿐 아니라 progress / status / cooldown / tooltip 전체**를 permit 전용 분기로

> ⚠️ 라벨만 `"토글"`로 바꾸면 여전히 거짓 카드다. `lingpet_rail_card.gd:217`이
> `duration = maxf(1.0, ...)`으로 **최소 1초 쿨다운을 강제**하고 거기서
> `progress = 1 - remaining/duration` 충전 진행도와 `status`(charging/ready/casting)를
> 만들며, `:316` 툴팁은 `cooldown_seconds`가 0 이하면 기본값으로 되돌려 **쿨타임
> 문구를 반드시 출력**한다(확인). permit은 진행도 막대도, 쿨타임 줄도 없어야 한다.

분류는 스킬 메타데이터의 새 필드로 선언한다(암묵 분기 금지).

### 2-4. 탑승 런타임 소유권은 스킬 호스트로 옮기지 않는다

**D1은 슬롯 경제만 뒤집었지 런타임 소유권까지 뒤집지 않았다.** idle 게이트는
`lingpet_companion_skill_controller.gd:95`의 **슬롯 루프 안에서만** 적용되고
(`can_skip_idle` → `continue`로 그 슬롯의 스킬 업데이트만 건너뜀), 실제 탑승은
그 바깥 `_update_companion_motion`(`lingpet_egg_runtime.gd:2824`)에서 **항상**
갱신된다(확인). 따라서 탑승 생존 술어를 `needs_runtime_update_for_skill()`에
등재할 이유가 없다 — 넣으면 소유권만 흐려진다.

확정 계약:

- `baekrin_saddle` = 액티브 슬롯을 점유하는 **`interaction_permit`**
- **현재 액티브 슬롯에 장착된 라이브 permit이 있을 때만** 탑승 허용
- 자동 무장 / launch / skill-host update 대상에서 **명시적으로 제외**
- 탑승 상태와 전이 정리는 계속 `lingpet_mount_state`가 **단독 소유**
- egg runtime이 장착 id와 온이마루 유예를 해석해 **`mount_permitted` 하나만** 전달
- `needs_runtime_update_for_skill()`에는 탑승을 **넣지 않는다**

> ⚠️ **"해금"의 정의 = 현재 액티브 슬롯에 장착.** 단순 획득/보유가 아니다.
> 보유만으로 성립하면 슬롯 예산이 무의미해지고, 슬롯을 다른 스킬로 바꿔도
> 탑승이 남는다. §2-1의 맵도 "보유"가 아니라 **장착 슬롯 조회**로 구현한다.

**permit 철회 전이 (진입 게이트가 아니다).** "현재 장착된 permit만 유효"라면, 탑승
중에 안장 슬롯이 제거·교체되어 `mount_permitted`가 false가 된 **그 프레임에 강제
하차**해야 한다. 이건 새 패턴이 아니라 기존 계약 그대로다 — `advance()`는 이미
미지원 펫 / 컴패니언 비활성을 **진입 게이트가 아니라 기존 탑승의 철회 전이**로
처리한다(`lingpet_mount_state.gd:126` 부근: `if not MOUNT_SUPPORTED_PET_IDS.has(_pet_id)
or not companion_active:` → 탑승 중이면 `_dismount()` + `toggled=true`, 입력 엣지
처리보다 **먼저** 반환, 확인).

따라서 `mount_permitted`는 **`advance()`의 인자로 넘겨 같은 철회 조건에 합류**시키고,
처리 소유권은 egg runtime이 아니라 **`lingpet_mount_state`**가 갖는다. egg runtime은
장착 슬롯을 조회해 불리언 하나를 만들어 넘기는 역할만 한다.

씰 계약: **실제 슬롯 조회 → `_update_companion_motion()` → 같은 프레임 `mounted=false`**,
그리고 **슬롯을 유지한 대조군은 계속 탑승**. (대조군이 없으면 "항상 하차"와 구별 불가)

별건 문서 결함(이 슬라이스와 무관하지만 S0에서 같이 고친다): `CLAUDE.md` 트랩
스텁과 `docs/godot_runtime_traps.md`가 게이트 생존 소스를 `is_launch_blocked`라고
적었으나, shipped 코드 주석이 그 재사용을 **명시적으로 금지**하고
`needs_runtime_update_for_skill()`을 지정한다. 문서를 따라 등재하는 다른 스킬이
조용히 죽는다.

### 2-5. 슬롯 예산과 module 충돌

`lingpet_active_skill_slot_resolver.gd:52-69`(확인): 2번째 슬롯은
`is_second_active_unlocked()`(수호령강화 보상) **AND** `get_active_slot_count() >= 2`
**AND** 2번째 skill id 비어있지 않음 — 3중 조건. 게다가 두 스킬이 같은 모듈을
공유하면 `_would_share_module`로 슬롯이 **조용히 2→1로 붕괴**한다.

D1이 "해금되어야 탑승"이므로 이 진행 게이트는 오히려 의도에 부합한다. 다만
부화 직후에는 안장·목린변신 중 하나만 장착 가능하다는 점을 기획에 반영해야
하고, 두 스킬의 `runtime_kind`가 겹치지 않도록 설계해야 한다.

---

## 3. D3(탑다운)이 만드는 에셋 / 렌더 결과

현행 `companion_carry`는 **정면 직립 이족 목말 25f** (`onimaru_companion_carry_idle_25f`)
이고, 애니메이터가 그 규격(25f 5×5)을 전제로 지오메트리를 재사용한다. 탑다운은
규격이 다르므로 **신규 렌더 분기가 선행**한다 — 탑다운 스틸을 기존 carry 슬롯에
넣을 자리가 없다.

| 에셋 | 상태 | 작업 |
|---|---|---|
| `백린_변신전`, `묵린` (정면 3/4) | ⭕ 즉시 | 누끼 → `cutin_art` / `concept_art_path` |
| `백린_빈안장_topdown` | 🔶 | 누끼 → 탑승 **M-베이스**. 탑다운 렌더 분기 선행 |
| `백린/묵린 위보는모습` (무안장 탑다운) | 🔶 | 비탑승 탑다운 베이스 |
| `백린_탑승` / `목린_탑승` | ❌ 직접 사용 불가 | 제3의 라이더가 베이크됨(플레이어블 5인 불일치) + M×N 조합 = 소켓 계약 위반. **탑다운 라이더 포즈 규격 참조**로만 |
| 캐릭터별 탑다운 착석 라이더 ×5 | ❌ 전무 | **N-세트 신규 제작** |
| `백린_목린_변신_18프레임` | ❌ 그대로 불가 | 행3(f13~18)에서 **꼬리가 사라짐** → f12→f13 재생 팝. **행3 버리고 12프레임 사용** 또는 꼬리 구간만 재생성. (열 6/행 3 식별 가능, 펫별 cols 오버라이드 선례 `rabi: 6` 존재 — 비균일 그리드/스케일락 위반 주장은 철회됨) |
| `companion_walk` / `strike` / `cast` | ❌ 전무 | AutoSprite 뒷모습. **라비 선례처럼 1장 재사용 가능**(walk/strike/cast 동일 파일) |
| `cutin_anim` 32f / `dismiss` / `click_reaction` | ❌ 전무 | dismiss는 click_reaction 재사용 가능(라비 선례) |
| 스킬카드 ×2 | ❌ 전무 | **없으면 프리웜 스모크 즉시 RED** |
| 알 3키 | ⭕ | 공용 재사용 = 무비용 |

절약 **후보**(확정 아님): 묵린은 백린과 실루엣 IoU 0.9967이라 시트를 백린만 만들고
5초 변신을 리컬러로 처리할 수 있어 보인다.

> ⚠️ 앞서 근거로 든 `onimaru_*_amber`는 **런타임 리컬러 선례가 아니다 — 철회한다.**
> 온이마루는 amber를 **별도 PNG로 카탈로그에 연결**하고(`lingpet_catalog.gd:965`
> 부근, cutin / dismiss / click만 `_amber` 변형이며 컴패니언 시트는 아니다),
> 컴패니언 렌더러의 modulate는 `Color(1,1,1,alpha)`로 **RGB는 흰색·알파만**
> 적용한다(`lingpet_companion_renderer.gd:158`, 확인). 즉 현재 렌더 경로에는 색을
> 바꿀 수단 자체가 없다.

이 절약안은 아래 셋 중 하나가 검증돼야 확정된다:
1. 별도 리컬러 시트(가장 단순, 제작량 절반 절감은 사라짐)
2. 부위 마스크 기반 팔레트 셰이더(금색 장식 / 눈 / 안장 보존)
3. 단순 tint로 금색 장식·눈·안장이 훼손되지 않는다는 **픽셀 증거**

미결: 온이마루 carry를 탑다운으로 재제작할지(§5-6).

---

## 4. 슬라이스 순서

| 슬라이스 | 내용 | 선행 이유 |
|---|---|---|
| **S0** | **트랩 문서 2곳 정정**(아래) + 정합성 재확인. 코드 0줄. 게임플레이 수치는 D5~D13으로 이미 닫혔다 | 틀린 문서를 따라 등재하면 게이트가 **에러 없이 조용히** 죽는다 |
| **S1** | **목린변신 단독 완주** | 자기완결·측정 가능. **5-1 + 5-2가 모두 닫힌 뒤** 착수 |
| **S2** | 안장 게이트 인프라: 펫→안장 스킬 맵(**장착 슬롯 조회**), 온이마루 유예, permit 분류 — **공유 쿨다운 양방향 면역**(시작·수신 모두), 자동무장 / trigger count / launch feedback 제외, 스냅샷에 `activation_model`·`interaction_active`·`interaction_available` 투영, 레일 **전면 분기**(progress·status·cooldown·tooltip), egg runtime이 `mount_permitted`만 전달 | D1의 본체. **탑승 상태·전이 정리는 `lingpet_mount_state` 단독 소유 유지**(§2-4) |
| **S3** | 탑다운 렌더 분기 + M(빈안장) + N(캐릭터 5종 착석) | D3. S2 없이는 진입점이 없다 |
| **S4** | 하차·원복 **단일 헬퍼** → 그 뒤에 per-pet 파라미터 승격(리프트·근접·속도·둔화·드레인) | 순서 반대면 래치 누수가 백린 도입과 동시 발생 |
| **S5** | 화면 지속시간 게이지 + `×2` 마커, 경고 임계 배율 스케일 | 2배 드레인의 **전제**(현재 관찰 수단 0) |
| **S6** | 드레인 배율 — `advance_duration` 안, 클램프 **뒤** 별도 인자 | 클램프 상한 1.0에 걸리면 무증상 no-op |
| **S7** | 전용 스킬 ≥1개 + 탑승 활성화 | 전용 스킬 없는 탑승은 순수 함정(D4) |
| **S8** | `오니마루의안장` 제작 → 유예 해제 | D2 |

S7 전까지는 **백린의안장을 획득 / 장착 후보에 노출하지 않아 `mount_permitted = false`가
유지**되도록 한다(S2 이후 구조에서는 화이트리스트가 아니라 **장착 게이트**가 기준이다).

### 씰 공통 요구

- 실제 게이트 / 호스트 관통. 유닛 `update()` 직접 호출은 공허 GREEN.
- 전이 계약은 **`_update_companion_motion` 관통**(`_mount_state.advance()` 직접
  호출은 전이 코드를 우회한다 — `9e0e7e4ec`에서 실제로 구멍을 통과시켰다).
- 대조군 필수. 없으면 "둘 다 미발동"으로 변별력 0.
- **permit 철회 레그 필수**(S2): 탑승 중 안장 슬롯 제거·교체 → 같은 프레임 `mounted=false`
  + 슬롯 유지 대조군은 계속 탑승. 진입 게이트만 씰링하면 철회 구멍이 남는다.
- 비기본 값 사용: 패들 폭·패들 높이·지속시간 잔량을 기본값으로 두지 않는다.
- 결과 단언은 플래그가 아니라 수치(실속도 px/frame, 변위 px, 웜픽셀 수).
- 전량 **in-place Edit 토글로 RED 반증**. ⚠️ `git reset`/`checkout`/`stash` 금지.
- 광역 회귀는 **멈추지 않는 전수 러너**로. 표준 `run_smoke_tests.ps1`은 첫 실패에서
  throw하므로 선행 RED가 있으면 뒤쪽 씰이 구조적으로 미실행된다.

---

## 5. 미결 (사용자 결정 필요)

> ~~5-1과 5-2는 결합이므로 결합 시뮬로 동시에 닫는다~~ — **철회.** 실제로는
> 5-1이 P0로 닫히면서(D6) 쿨다운이 유일한 레버가 되었고, 5-2는 시뮬 곡선이 아니라
> **구조값 + 카탈로그 분포 + 사용자 판단**으로 닫혔다(D7~D9). 결합 스윕은 5-1의
> 캡 구간 사실을 밝히는 데 기여했으나 5-2의 결정 근거는 아니었다.

### 1차 시뮬 결과와 그 한계 (2026-07-31, SHA `6A73E10E…`, 904행, 2281초, 시드 200)

⚠️ **시뮬 자체 결함(내 쪽)**: `GLOBAL_CAPSTRIP`을 `policies`에서 분리한 뒤 **어떤
표에서도 실행하지 않았다** — 출력 0행. **미실행 진단 arm**으로만 명시하고 어떤
결론에도 쓰지 않는다.

★★ **4B 쿨다운 축의 성립 범위 — "무효"가 아니라 "상시 소환 상한 시나리오"다.**
`lingpet_companion_skill_persistence.gd:94-100`(직접 확인):
\`\`\`gdscript
# Guardian cooldown time only exists while a guardian is actually summoned.
if not companion_active:
	return
\`\`\`
`advance_states`(:122-125)도 동일 가드다. **수호령 쿨다운은 벽시계가 아니라
"소환된 시간"으로만 흐르고**, 스토우/재소환을 가로질러 보존된다.

⇒ 4B는 벽시계 + 상시 소환을 가정했다. **상시 소환이면 소환시간 == 벽시계**이므로
4B의 발동 횟수는 **그 조건 아래서는 성립한다** — 전면 무효가 아니라 **상한
시나리오**로 읽어야 한다. 실제 플레이에서 소환 duty가 1 미만이면 발동 횟수는 그만큼
줄어든다.

⇒ 구조값 **조작 박탈 비율 = 5 / cd** 도 무조건이 아니다. 이는 **소환 활성시간 대비**
비율이고 **즉시 재사용·무풍업**을 가정한다. **벽시계 기준 비율은 `소환 duty × 5/cd`
이하**다. D9의 "12.5%"는 **소환 활성시간 기준**이라는 뜻이다.

살아있는 결론(관측 사실):
- **관측된 라이브형 구간에서는 신규 +15%가 클램프되어 식별 가능한 이득이 없었다.**
  캡 26/31에서 가드 접촉이 전부 캡 초과(508/508, 518/518)라 생존 증분 +0.000.
  ⚠️ 캡 36 행의 증분 +1.060은 **`rally_speed_cap_bonus`를 인위적으로 주입한
  상태**의 값이다(정상 플레이에서 한 랠리에 20타를 쌓아야 도달, 서브마다 0 리셋).
  라이브 재현성은 별도 확인이 필요하다.
- ⚠️ "P2는 항상 무의미"도, **"라이브 전 구간에서 물리적으로 불가능"도 과잉 주장이라
  철회**한다. 배율 경로 자체는 정상 작동하고(신령환 캡해제 시 raw 25.37→33.13),
  아래 "닫힌 형태" 논증에는 숨은 가정이 있다.

⚠️ **P2 기각 근거의 강도 정정 (중요).** 이전에 적었던 "타구 배율 1.07288 고정 ⇒
12조합 전부 구조적으로 정확히 0"은 **닫힌 증명이 아니다**:
- `target_x`는 **목표일 뿐 중앙 접촉 보장이 아니다** — 자동조종은
  `move_toward(player_pos.x, target_x, max_speed * fps_scale)`로 **프레임당 최대
  24px**만 이동한다(`active_item_aipill_behavior.gd:20-25`).
- 타구 배율은 고정값이 아니라 **랜덤 기본 배율 × 속도 감쇠 × 위치별 보정의 합성**
  이다(`paddle_bounce_speed_multiplier_resolver.gd:33` `get_scaled_random_multiplier`
  → `apply_dampened_multiplier` → `_apply_edge_hit_multiplier`).
- 제시했던 부등식 `mult < 1 + 0.5/cap`은 **정책 적용 직전 속도가 이미 현재 캡과
  같다**는 가정을 숨기고 있다. 실제 생존 조건은 **정책 직전 유효속도**까지 포함해야 한다.

⇒ 따라서 P0 채택(D6)의 근거는 "수학적으로 0임이 증명됨"이 아니라
**"관측 구간에서 이득이 식별되지 않았고, 리포 선례상 접촉 공속 배율 3종이 전부
캡 우회와 세트라 캡 준수 배율은 전례가 없다"** 이다.

⚠️ **"창 내 무실점"도 보증이 아니다.** 시뮬의 창 내 실점 0.000을
"`|vx| ≤ 26·cos25° ≈ 23.6 < 자동조종 24`이므로 항등적으로 무실점"이라고 쓰면
과잉이다 — 그 부등식은 **추적 오차가 늘지 않는다**는 조건에 가깝고, 초기 위치 오차·
남은 비행시간·패들 허용폭 없이는 캐치를 보장하지 않는다. 정확한 표기는
**"이 모델·이 시드 집합에서 0건"** 이다. 라이브 문구로 "5초 무실점"을 쓰지 말 것.

승격 금지 / 미확정:
- ⚠️ **P3(캡 해제)의 `+4.1577 Δnet/min`은 라이브 효과로 승격 금지.** 시뮬이 보스
  대쉬를 미모델인데, 실제 대쉬는 **일반 이동으로 못 닿을 때만** 발동하고
  (`_try_start_boss_dash`: `frames_to_contact = vertical_gap / (|vel.y|*boost)`,
  `if required_distance <= effective_speed * frames_to_contact: return false`),
  `frames_to_contact`는 공속에 반비례한다. 즉 **P3가 만드는 조건이 곧 대쉬 발동
  조건**이라 구조적 상향 편향이다. 발동률 0.30 / 리미트 0.60 / 신화 1.0,
  토큰 1 / 2 / 신화 S5 3개(+연쇄).
- ⚠️ **P0 vs P2 동등, P1 열세는 통계적으로 미확정.** P2−P0가 15초 +0.0060,
  55초 −0.0027로 **부호가 뒤집힌다**. 시드별 paired difference 분산·신뢰구간이
  없다. 현 표본으로는 "구분 불가"가 정확한 표현이다.

### 대쉬 경계 재실행 프로토콜 (P3 판정용, 착수 전 필독)

- **DiD 필수**: `P0/dashOFF`, `P0/dashON`, `P3/dashOFF`, `P3/dashON` 4칸을 **같은
  시드**로 돌리고 핵심값은 **(P3_ON − P3_OFF) − (P0_ON − P0_OFF)**.
  P3 ON−OFF만 보면 일반 대쉬 이득과 P3 특화 구제량이 섞인다.
- **실제 경로 관통 필수**: 수동 대쉬 근사 금지. `BossAiState.update()`
  (`scripts/ai/boss_ai_state.gd:626`의 `_try_start_boss_dash` 분기)를 관통해야 한다.
  ⚠️ 현재 시뮬은 `BossAiPredictionState` + `BossAiTurnInertiaResolver`만 조립해 쓰므로
  **보스 이동 루프를 실제 `update(delta, boss_pos, boss_vel, context)`로 교체**하는
  작업이 선행한다(작은 패치가 아니다).
- ⚠️ **BossAiState는 포인트 단위 무상태가 아니다.** 토큰·충전 타이머·대쉬/스턴·
  서브페인트·예측 상태가 전부 **인스턴스 변수**다(`boss_dash_tokens`,
  `boss_dash_charge_timer_frames`, `boss_dash_recharge_frames`,
  `boss_dash_stun_*`, …). 포인트마다 AI를 새로 만들면 토큰과 충전이 매번
  초기화되어 **대쉬가 과다 공급**된다. 따라서:
  - 단일 포인트 DiD라면 **"첫 포인트 경계 실험"으로 한정**해 보고한다.
  - Δnet/min까지 재평가하려면 **세션 시드당 AI 인스턴스 1개를 유지**한다.
  - **1.5초 재서브 구간도 `waiting_for_serve=true`로 실제 업데이트**해 충전 시간을
    진행시켜야 한다(안 그러면 토큰이 공짜로 찬다).
  - 이 경우 **기존 포인트 캐시는 폐기**한다(AI 상태까지 키에 넣는 것은 비현실적).
    ⇒ 1차 실행의 38분은 캐시 덕분이었으므로, 대쉬 재실행은 **경계 실험 범위로
    좁혀야** 한다(전체 쿨다운 스윕을 캐시 없이 돌리지 말 것).
- **함께 출력**: 대쉬 적격 프레임 수, 발동 수, 연쇄, 토큰 소비, 캐치 전환.
  ⚠️ **적격 여부는 외부에서 관측 불가**다 — `get_dash_token_snapshot()`은
  tokens / max_tokens / charge_timer / recharge_frames / charge_progress /
  available_timer / recovery만 노출하고 **적격 플래그가 없다**. 외부에서 게이트
  조건을 복사하면 "수동 근사 금지"와 충돌하므로, **실제 게이트 내부에
  행동 중립 테스트 프로브**를 심어 eligible / start / chain을 기록한다.
  ⚠️ 그 스냅샷 Dictionary는 **프레임당 재사용되는 공유 참조**다(코드 주석 명시).
  프레임을 넘겨 저장하려면 반드시 복사할 것.
  ⚠️ 단순히 "대쉬 후 캐치"를 세면 **인과적 전환이 아니다**. paired 반사실로
  증명하지 못하면 열 이름을 **`dash-following catch`** 로 낮춘다.
- **컨텍스트는 운영 순서를 따른다**: 손조립이 아니라
  `build_boss_ai_context(owner, registry)` → `ai_state.update(delta, ...)`
  (`battle_scene_actor_update_driver.gd:61` 순서).
  ⚠️ **round-state 픽스처가 없으면 빌더가 `waiting_for_serve=true`로 폴백**하고
  (`_is_waiting_for_serve`: `round_state == null or not has_method(...) -> return true`),
  `_try_start_boss_dash`가 그 플래그에서 즉시 false를 반환해 **대쉬가 조용히 전부
  막힌다**. 랠리 구간에서 `waiting_for_serve == false`임을 **반드시 단언**할 것 —
  안 하면 "대쉬 효과 없음"이라는 거짓 결론이 나온다.
- **조합**: champion S1 + mythic S5 두 끝점이면 경계 검증으로 충분하다. 단
  **두 끝점이 같은 방향의 큰 상호작용을 보일 때만** P3 기각 근거가 된다.
  방향이나 크기가 크게 다르면 **중간점 limit S3를 추가**한다.
- **paired CI 출력**을 함께 넣어 §5-1의 통계 미확정 항목도 같이 닫는다.

| # | 항목 | 왜 막혀 있는가 |
|---|---|---|
| ~~5-1~~ **닫힘 → D6** | **가드당 공속 +15% 처분 = P0 채택, 원안 +15% 기각.** 근거는 **닫힌 증명이 아니다**(그 논증은 §1차 시뮬 절에서 철회): ① 관측된 라이브형 구간(캡 26/31)에서 가드 접촉이 전부 캡 초과라 생존 증분 +0.000 — **이득이 식별되지 않았다**, ② 리포 선례상 접촉 공속 배율 3종(신령환·야생의포효·자폭드론)이 **전부 캡 우회 플래그와 세트**라 캡 준수 배율은 전례가 없다. ⚠️ "타구 배율 1.07288 고정", "항상 중앙타", "물리적으로 불가능"은 **전부 철회**(배율은 랜덤×감쇠×위치 합성, 자동조종은 `move_toward` 목표일 뿐 프레임당 24px 상한) | `_cap_effective_velocity`는 **캡 초과일 때만** 크기를 버린다(`effective <= cap`이면 조기 반환) — 이전에 쓴 "캡 유지 시 배율 소멸"은 **과도한 일반화이며 철회**한다. 캡 **미만 / 경계 / 초과** 3구간을 분리해 평가해야 한다. 부호도 미정: 실수 **빈도**는 접근 1회당 공속 무관이지만 **고정 시간당 접근 횟수**는 공속에 따라 변한다. 실수 **크기**는 `clamp(speed × scale, err_min, err_max)`이므로 **비포화 구간에서만 정비례하고 리그별 floor/cap에서 소멸**한다 — 기본 78..140(정비례 구간 공속 17.33~31.11) / 리미트 40..80(8.89~17.78) / 신화 0..20(0~4.44), scale은 셋 다 4.5. 즉 **신화는 실전 공속 전 구간에서 이미 포화(항상 20px)**라 공속→실수크기 채널이 죽어 있고, 리미트도 공속 17.78 이상에서 죽는다 |
| 5-2 | **목린변신 쿨다운 값** | 기획에 없음. 쿨다운 없이는 파워 평가가 원리적으로 불가. **숫자만으로는 안 닫힌다** — 아래 두 입력이 선행한다 |
| 5-2a | **균형 기준 한 축**: 허용 목표 Δnet/min **또는** 비교 대상 기존 스킬 (둘 중 하나) | 비교 스킬을 택하면 **동일한 세션·리그·스테이지·측정 단위**로 평가해야 한다(다른 단위로 비교하면 무의미) |
| 5-2c | **판정 지표를 `net/min`이 아니라 포인트당 승률 상승폭으로** | Δnet/min의 82~100%가 "이겨서"가 아니라 **"포인트를 덜 치러서"**다(신화 S5는 100%). net/min을 쓰면 랠리를 늘리는 능력에 과잉 보상한다. 실측 승률 상승: champion +11.5~25.5pp / junior +8.0~17.0pp / limit +1.5~13.5pp / **mythic 0** |
| 5-2d | **조작 박탈 허용 상한** (이 값이 곧 쿨다운: `cd = 5 / 허용비율`) | 25%→20초 / **17%→30초** / 12.5%→40초. 카탈로그 액티브 24종의 **중앙값·최빈값이 30초**, 같은 방어 대행 가족은 유령소환 40 / 영혼분신 55(단 지속 15~20초) |
| 5-2b | **리그·스테이지 집계 규칙**: 실제 이용률 가중 / 동일가중 / 최악 셀 제한 중 무엇인가 | 이걸 안 정하면 **같은 결과로도 쿨다운이 달라진다**. 1차 시뮬의 대표 5조합 동일가중 평균은 실제 플레이 분포가 아니다. 반응지연 순위 안정성도 champion S1 범위 한정 |
| 5-3 | 탑승 **최소 유지시간** / 진입 비용 | 없으면 2배 드레인이 페널티로 미작동(최적 1.0~1.4% vs 순진 40~57%). 형제 계약: 수호령 소환 = 최소유지 6초 + 재소환 10초 |
| 5-4 | **속도 8의 강도** — 뿔딸기식 base 치환(권장) vs 배율 무시 하드 고정 | 후자는 신규 훅 필요. 그리고 `paddle_accel` 0.5 고정 미변경이면 12프레임 내 이득 0 = 실질 너프 |
| 5-5 | **조작 스킴** — RMB 유지 vs 전용 키. 게임패드 경로 | 코만도 RMB 이중발동, 스매셔 벽력유성 시 엣지 소멸, 게임패드 단독 버튼 0개(R3는 소환 점유) |
| 5-6 | 온이마루 carry를 탑다운으로 재제작할지 | D3가 표준이면 현행 정면 목말이 비표준으로 남는다 |
| 5-7 | 탑승 중 대쉬 허용 여부 | `DASH_BASE_SPEED 40` + `update_horizontal` 스킵으로 둔화를 100% 우회 |
| 5-8 | A+B 중첩 시 조작 우선순위 | 목린 자동가드는 패들 자동조종(4배·관성 0)이라 탑승 조작감을 대체한다. "모드 전환"으로 수용하면 정상이지만 명시 필요 |
| ~~5-9~~ **닫힘 → D12** | 표기 확정: 플레이어 표기 **`묵린`**, 내부 ID **`mokrin`** | |

---

## 6. 선행 처리 완료 / 별건

- **완료**: 온이마루 탑승 회귀 4건(`player_paddle_size` 스키마 미선언, 선제타격
  마운트 미조회, 방어 인터셉트 래치, 클릭 교감 — 신규 + 진행 중 양방향).
  `07518d1b1`, `9e0e7e4ec`. 라이브 픽셀 QA 3항목 통과.
- **미확정**: 홉 진행 중 라이더 스프라이트와 carry 본체의 정밀 수직 정렬
  (운영 스테이지 렌더러 필요).
- **별건 선행 RED 8건**(본 작업 기인 0): `lingpet_audio_dispatcher`
  (egg_runtime:814 `get_instance("game_audio")` 잔재), `guardian_spirit_rebrand`
  (**Parse Error** — `LINGPET_AFFINITY_CHIP_PERK` 부재, 지속시간 대개편 고아 참조),
  `game_audio_lingpet_combat_audio_owner`, `lingpet_debug_picker`,
  `lingpet_orosha_debug_l2d`, `lingpet_permanent_collection_surface`,
  `lingpet_runtime_vector_resolver`, `lingpet_snapshot_sync_gating`.
  이 커밋들과 섞지 않는다.
