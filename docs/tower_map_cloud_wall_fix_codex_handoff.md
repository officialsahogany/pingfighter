# 지시문 J3-수정 — 리빌 팝 + 최소 줌 반복감 보완 (관제탑 검토 회신)

- **발행**: 관제탑 2026-08-24. 대상: 브랜치
  `codex/tower-map-cloud-wall-20260824`(워크트리
  `D:\codex_tmp\bosspong_cloudwall_e757`)의 `a2679b9e2` 위 **추가 커밋**.
  기존 2커밋 리베이스/amend 금지. 본 트리 편집·푸시·통합 금지.

## [M1] 리빌 완료 디졸브 팝 (검토 실측 확정)

- 원인: `tower_ascent_map_cloud_layer.gd:334`
  `reveal_alpha = 1.0 - progress`가 디졸브 스트립 전체(:442~449)에
  걸려, 진행 중 스트립이 0까지 사라졌다가 완료 틱에 알파 1로 복귀 —
  224 world-px 띠가 한 프레임에 팝(중간 층 공개 전부 재현, 최종 층만
  면제). 부수: 꼬리 구간에서 잠긴 지대 일시 차폐 저하(경계 위
  224×scale 안에 잠긴 노드가 있으면 순간 판독 위험).
- 수리: **경계 아래(공개되는 구간)만 페이드**하고 스트립의 최종 안착
  지대는 불투명 유지(또는 크로스페이드) — 애니메이션 마지막 프레임 ==
  완료 후 정지 프레임. 중간 진행의 하드 스텝 라인도 같은 수리로 소멸.
- 씰: `wall_visual_state`의 progress→1 극한과 완료 후 정지 상태의
  디졸브 알파/rect 동등 단언 레그 신설(연속성 봉인 = 일시 차폐 저하도
  함께 봉인). 기존 팝 재현 픽스처 RED 반증.

## [보완] 최소 줌 타일 반복감 (관제탑 확정 방향)

1. **내부 타일 Y 월드 스케일 상향**(2~3배 후보 실측 비교): 단일
   상수를 신설해 `build()`의 interior world_size와
   `estimate_draw_calls`의 tile_height **두 소비처가 같은 정본을
   참조**하게(검토 실측: 현재 두 곳 독립 — 락스텝 필수). 예산 씰은
   자동 재파생(타일 커짐=콜 감소) 확인.
   ※X는 현재 스트레치 매핑이라 스케일 비표현 — X 확장은 이번 스코프
   밖(현행 유지), 시도하지 말고 Y만.
2. **저줌 모티프 보강**: 모티프 상수 배열 확장(개수 증가)과/또는
   저줌 조건 드로우(zoom은 camera_model에서) — 빌드 모델은 카메라
   독립 유지, reserve는 최대치 기준. `BITMAP_FRONT_CLOUD_COUNT`·
   모티프 씰·예산 락스텝 동반.
3. 판정 기준: 최소 줌 재캡처에서 세로 반복 인지 저감 — 기존 QA의
   `motif_break` 지표 개선치 보고 + 관제탑 육안 재검수용 fit-all
   캡처 1장(레퍼런스 나란히).

## [L] 동반 정리 (같은 커밋 가능)

- F2: `_draw_dissolve_with_join_blend`의 프레임당 Packed 배열 3종
  캐시/재사용, 또는 GRT-003 씰 메시지 스코프를 정직하게 축소.
- F3: 고아 자산 `cloud_haze_band_imagegen_v1_x4.png`(+.import) 제거 —
  에디터 무중단 경로(파일 교체·포커스 재임포트 규칙 준수) 또는 존치
  사유 명시.

## 게이트·보고

기존 씰 3종+신설 연속성 레그(+팝 RED 반증) → `-Paths` 경고 → 헤드리스
로드 → `git diff --check` → 캡처: 리빌 완료 전후 연속 2프레임·최소 줌
fit-all(레퍼런스 병렬)·중간 줌 1장. 보고=추가 커밋 해시·씰 종단선
원문·motif_break 지표 전후·Y 스케일 채택값과 근거·미해결.
