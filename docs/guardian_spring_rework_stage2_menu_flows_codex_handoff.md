# 지시문 S2 — 샘터 개편 2단계: 메뉴 상태기계 + 기도 램프 + 영접

- **발행**: 관제탑 2026-08-24. 기준=S1 착지 커밋(같은 워크트리
  `D:\codex_tmp\bosspong_spring_s1_63f8` 순차). S1 GREEN 선행 필수.
- **금지**: 본 트리 직접 편집·푸시·통합. 보고 후 대기.

## 확정 스펙 (사용자 판정 완료)

샘터 메뉴는 수호령 보유 여부로 2상태:

- **수호령 없음** (모든 샘터, 횟수 무제한):
  - `손바닥을 대본다` — 무료. 첫 수호령 즉시 획득 플로우(S3에서 구현;
    S2에서는 액션 자리와 게이트만 배선, S3 전까지 기존 영혼소환술 습득
    카드 동작 유지).
  - `기도한다` — 플레이어 본인 캐릭터 전능력치 +3% (수호령이 아님).
    비용 램프: 런 내 1회차 무료, 2회차 2무혼, 3회차 4무혼, +2씩 증가.
    런 종료/시작 시 초기화. 수호령 획득 순간 이 버튼은 영구 소멸.
- **수호령 있음**:
  - `수호의 신과 영접한다` — 무혼 1 (현행 2에서 인하, 사용자 확정).
    기존 랜덤 강화 롤+컷인 그대로(랜덤 유지는 스킬레벨 편중 방지 의도).
    반복 가능(현행 sequence 번호 방식 유지).
  - `수호령들을 살핀다` — S3에서 구현. S2에서는 자리만.

## 작업

1. `tower_ascent_guardian_spring_node.gd` 메뉴 상태기계 개편: 보유 여부
   분기 + 기도 액션 신설 + 영접 비용 상수 인하
   (`TEMP_PHASE_C_SPRING_ENHANCE_COST` 2→1, 튜닝 정본에서).
2. **기도 프리미티브 신설** (전능력치 % 프리미티브 부재 실측):
   - run_state에 `prayer_count`(int) 신설 — ⚠GRT-017: 스냅샷/owner 투영
     대상 키는 스키마 선언 필수. 램프 비용
     `TEMP_SPRING_PRAYER_COST_STEP := 2` 파생(`(count) * STEP`, 1회차
     0) — ⚠GRT-054: 리터럴 금지, 상수 파생.
   - 효과 적용은 `runtime_perk_effective_stat_query_surface.gd` 경유
     전역 가산: % 계열 스탯에 `+3.0 * prayer_count`%p, flat 계열
     (max_gauge 등)은 기준값의 3% 비례 가산. 기존 per-stat 상한
     (쿨감 95% 등) 준수. 모델=training_mastery 배율 전례.
   - 표시면: 능력치 패널/TAB에서 증가분이 실측 반영되는지(GRT-017
     divergent 레그) + 수련 보너스와의 합산 순서 명시.
3. 기도 소멸 게이트: 수호령 획득 이벤트(알 부화 귀속 포함) 시
   `prayer_locked` 플래그 — 이후 샘터에서 기도 미노출. 램프 카운트와
   플래그 모두 런 리셋.
4. ⚠GRT-058: 영접이 여는 강화 컷인의 개폐 부수효과(모달 시간 동결
   09006a8b1 계약)는 기존 훅 소유 유지 — 새 진입점에서 중복 호출 금지.
5. 카피 4개 국어(ko 우선, 한국어 엠대시 금지): 기도 결과 문구·램프
   가격 표기·기도 소멸 후 상태 문구.

## 씰

- 상태기계 레그: 무보유→[손바닥/기도], 보유→[영접/살핀다 자리],
  획득 후 기도 미노출, 램프 0/2/4 비용 실제 차감, 런 리셋.
- 기도 효과 레그: prayer_count 2에서 효과 스탯 쿼리 표면이 +6%
  divergent 값 반환(GRT-017 스키마 관통) + 상한 스탯 클램프 레그.
- 영접 레그: 무혼1 차감·랜덤 롤 발동·반복 sequence — 기존 spring
  스모크 개정.
- RED 반증: 램프 상수 0 강제 시 비용 불변 RED / prayer_locked 강제 후
  기도 노출 RED.
- 게이트: 포커스드 스모크 → `-Paths` 경고 → 헤드리스 로드 → diff →
  기도 2회 적용 상태 능력치 패널 Vulkan 캡처 1장.

## 보고

단계 커밋 해시·씰 종단선·캡처 경로·미해결. GREEN 후 같은 세션 S3.
