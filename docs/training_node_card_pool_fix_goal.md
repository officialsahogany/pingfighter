# 수련장에 무공 카드 혼입 /goal 지시문 (2026-08-21)

- **출처**: 사용자 라이브 제보.
  > 수련장은 **수련 카드만** 나타나야 하는데 강신결·장기심법·역천호신 같은
  > **무공 카드가 같이** 나온다.
- **기준 HEAD**: 최신. ⚠ 미커밋 WIP 다수. 격리 워크트리에서 작업하고
  `stash`·`checkout`·`reset`·통짜 `git add` 없이 통합 대기하라.
- **완료 보고**: `docs/training_node_card_pool_fix_report.md`. 푸시 금지.
  **통합하지 말고 보고 후 대기하라.**

## 0. Fable이 확인한 정본 `[확인]`

`docs/tower_ascent_run_map_plan.md` §3.10 (v1.10, 사용자 확정 2026-08-18)

```
- 수련장 = 수련 카드 6장 제시.
- 파계승 = 무공·초식 카드 6장 제시.
```

**수련장은 수련 6장이다. 무공은 파계승 소관이다.**

★**구식 개정이 남아 혼동을 만든다.** 같은 문서 v1.3 에 "수련장 무공 서가"가
있고, 현재 모달 부제도 "무혼을 다듬어 **몸과 무공을** 수련합니다"다.
**v1.10 이 나중이며 그것이 정본이다.**

`tower_ascent_training_offer_builder.gd` 가 `_build_stat_choices()` 와
`_build_mugong_choices()` 를 둘 다 만들어 섞는다. 그것이 결함이다.

## 1. 계약

- 수련장 오퍼를 **수련 6장**으로 바꾼다.
- ★**파계승을 깨뜨리지 마라.** 무공·초식 6장은 그쪽 계약이다. 같은 빌더를
  공유하면 노드 종류로 갈라라.
- ★**모달 부제 문구도 고쳐라.** "몸과 무공을 수련합니다"는 구식 사양의
  잔재다. 새 문구는 판단해서 정하되 **한국어 카피에 엠대시(—) 금지**이고
  **7 로케일 등재**가 필요하다.
- ★**중복 투자 상한은 폐지된 상태다**(사용자 확정). §3.10 의
  `TEMP_PHASE_C_TRAINING_USES_PER_VISIT := 2` 문장은 **이미 폐기됐다.**
  되살리지 마라.
- 수련 항목이 6개를 못 채우면 어떻게 할지 판단하고 근거를 적어라.
  포화된 항목을 제외하면 후보가 모자랄 수 있다.
- ★**기획서 §3.10 을 개정하라.** v1.3 의 "수련장 무공 서가"가 살아 있어
  다음 사람이 또 혼동한다. 폐기 표시를 달아라.

## 2. 씰

- 수련장 오퍼에 **무공·초식이 하나도 없는지** 단언하라.
- 수련장 오퍼가 **6장인지** 단언하라.
- **파계승 오퍼는 무공·초식 6장 유지** 부정 레그를 둬라.
- 후보 부족 경계를 단언하라.
- ⚠ 씰 레그를 추가하면 `_leg_count` 류 상수를 함께 갱신하라.

## 3. 규율

- 헝크 분리 커밋. 단독으로 씰 GREEN.
- 신규·개정 씰은 두 리터럴 목록에 동시 등재. 현재 193개다.
- 통과 판정은 배치 종단선 `All Godot smoke tests passed.` + `SCRIPT ERROR` 0건.
- 로그 백업 선행. 표준 래퍼 `-AllowDuringPlay`. 푸시 금지.

## 4. 검증

- 씰 + `-Paths` 경고 + 헤드리스 + `git diff --check`.
- **Vulkan 캡처**: 수련장 6장, 파계승 6장.
- **라이브 확인은 사용자가 본 트리에서 한다.** unverified 로 명시하라.

**완료 선언 조건**: 구현·검증 + 기획서 개정 + 게이트 blocked 0건.
