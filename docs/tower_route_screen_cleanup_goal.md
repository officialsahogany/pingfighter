# 경로 선택 화면 잔재 정리 /goal 지시문 (2026-08-21)

- **출처**: 사용자 라이브 제보 4건. 전부 **같은 화면**의 문제다.
  1. 스테이지1에서 경로 공 발사 화면에 **직전 오브젝트(풍선)가 잔뜩 남아**
     화면이 지저분하다.
  2. 수련장에서 나온 경로 선택 화면에 **이전 보스(달지)의 스킬카드 2장이
     남아** 있다.
  3. **플레이필드 테두리가 없다.** 필러 배경만 있고 공이 움직이는 상자의
     경계가 안 보인다.
  4. **플레이어 캐릭터가 안 보인다.**
- **성격**: ★**오늘 착지한 `2bc0561be` 의 후속이다.** 그 커밋이 선택 공만
  좁게 그리도록 고쳤는데, 화면을 이루는 나머지가 빠졌다.
- **기준 HEAD**: 최신. ⚠ 미커밋 WIP 다수. 격리 워크트리에서 작업하고
  `stash`·`checkout`·`reset`·통짜 `git add` 없이 통합 대기하라.
- ★**새 워크트리를 콜드로 만들지 마라.** 하나가 약 10GB다.
- **완료 보고**: `docs/tower_route_screen_cleanup_report.md`. 푸시 금지.
  **통합하지 말고 보고 후 대기하라.**

## 0. 배경 `[확인]`

`f4adbea3b` 가 비전투 노드에서 이전 스테이지 아레나를 숨겼다.
`2bc0561be` 가 그 상태에서 경로 선택 공만 다시 그리게 했다.

`battle_scene_drawer.gd` `_draw_tower_ascent_playfield_flow_only()` 가
`ROUTE_AIM` 일 때 `draw_tower_route_selector_ball()` 만 부른다.

**공은 돌아왔지만 화면의 나머지가 빠졌다.** 반대로 전투 스테이지가 소유한
잔재(풍선·스킬카드)는 억제되지 않았다.

## 1. ★먼저 판정하라 — 이 화면은 무엇을 보여야 하는가

정본은 `docs/tower_ascent_run_map_plan.md` §3.2 다. **v1.6 에서 "별도 발사
화면 폐지·실제 서브·랜덤성 복원"으로 개정됐다.** 반드시 읽어라.

- 경로 선택 서브 화면의 **의도된 구성 요소를 목록으로 확정**하라.
  플레이필드 테두리, 플레이어, 표적, 공, 필러 HUD 중 무엇이 남고 무엇이
  빠지는가.
- ★**지금은 "전투 화면 전체" 와 "선택 공 하나" 두 극단뿐이다.** 그 사이에
  올바른 집합이 있다. 그것을 정의하는 것이 이 목표의 본체다.
- 판정 결과를 보고서 맨 앞에 표로 적어라.

## 2. 계약

### 2-1. 빠진 것을 되살린다

- **플레이필드 테두리**와 **플레이어 캐릭터**를 그린다.
- ★**전체 `_draw_playfield_scene` 을 되돌리지 마라.** 그러면 이전 보스와
  아레나가 같이 돌아와 `f4adbea3b` 가 고친 것이 깨진다.
- 필요한 드로어를 개별로 지명해 호출하라. `battle_playfield_scene_drawer.gd`
  의 소유자 경계를 먼저 확인하라.

### 2-2. 남은 것을 지운다

- **스테이지1 풍선 등 전투 오브젝트**가 경로 화면에 남는다. 무엇이 그리는지
  파일:줄로 지명하라.
- **이전 보스 스킬카드 레일**이 남는다. 스테이지별 카드 레일 렌더러가
  소유한다. 비전투/경로 화면에서 꺼라.
- ★**전투 복귀 시 정상 복귀**해야 한다. 부정 레그를 둬라.
- ★**detached FX·오디오 호스트**도 함께 확인하라. 논리적 가시성만 false 로
  두면 잔재가 남는다.

## 3. 씰

- 경로 선택 프레임에서 **플레이필드 테두리와 플레이어 픽셀이 존재**하는지
  단언하라.
- **이전 보스 픽셀 0**, **이전 스테이지 오브젝트 픽셀 0**, **스킬카드 레일
  픽셀 0** 을 각각 단언하라.
- ★**픽셀로 판정하라.** 구조 단언만으로는 부족하다. `2bc0561be` 가 남긴
  비헤드리스 QA 래퍼를 확장하라.
- **전투 복귀 무손상** 부정 레그를 둬라.
- ★**비탑 일반 캠페인 무손상** 부정 레그를 둬라. 공용 드로어다.

## 4. 규율

- 슬라이스마다 헝크 분리 커밋 1개. 단독으로 씰 GREEN.
- 신규·개정 씰은 두 리터럴 목록에 동시 등재. 현재 193개다.
- ⚠ 씰 레그를 추가하면 `_leg_count` 류 상수를 함께 갱신하라.
- ⚠ `battle_scene_drawer.gd` 는 전 스테이지 공용이다.
- 통과 판정은 배치 종단선 `All Godot smoke tests passed.` + `SCRIPT ERROR` 0건.
- 로그 백업 선행. 표준 래퍼 `-AllowDuringPlay`. 푸시 금지.

## 5. 검증

- 씰 + `-Paths` 경고 + 헤드리스 + `git diff --check`.
- **Vulkan 캡처**, 실 해상도(2020x1246).
  - 스테이지1 경로 선택 (풍선 없음, 테두리·플레이어 있음)
  - 수련장 복귀 경로 선택 (달지 스킬카드 없음)
  - 전투 복귀 정상
  - 비탑 캠페인 무손상
- **라이브 확인은 사용자가 본 트리에서 한다.** unverified 로 명시하라.

**완료 선언 조건**: §1 구성 판정표 + 2-1·2-2 구현·검증 + 픽셀 증거 +
게이트 blocked 0건 + 보고서 완성.
