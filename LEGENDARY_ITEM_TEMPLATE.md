# 전설 아이템 템플릿 가이드

## 🎨 필수 스타일 요소 (MANDATORY)

모든 전설 아이템은 **반드시** 다음 요소들을 포함해야 합니다:

### 1. 초대형 단색 원형 배경 (라그나로크 해머 스타일)
- **크기**: 28픽셀 반경 (32픽셀 아이콘 거의 전체를 채움)
- **색상 변화**: 프레임마다 색상이 변하는 8가지 톤
- **단색 스타일**: pygame.draw.circle로 진한 단색 원 (그라데이션 최소화)
- **위치**: 가장 먼저 그려서 전체 배경 역할
- **중요**: 배경은 아이템보다 먼저 그려야 함!

#### 배경 색상 예시 (아이템 테마에 맞게 조정)
```python
# 라그나로크 해머: 파란색 계열
bg_colors = [
    (100, 150, 255),  # 밝은 파란색
    (80, 120, 230),   # 중간 파란색
    # ... 8가지 색상
]

# 헤르메스의 신발: 청록색 계열  
bg_colors = [
    (50, 200, 150),   # 청록색
    (40, 180, 130),   # 진한 청록색
    # ... 8가지 색상
]
```

### 2. 은색/보라색 모서리 장식 (3x3 픽셀)
- 각 모서리에 3x3 픽셀 영역
- 프레임별로 색상 변화 (은색 ↔ 보라색)
- 왼쪽/오른쪽 모서리가 서로 다른 색상

### 3. 프레임별 변화하는 빨간 테두리
- 테두리 두께: 2픽셀
- 색상 변화 패턴:
  - Frame 0, 4: RGB(150, 0, 0)
  - Frame 1, 3: RGB(224, 0, 0)
  - Frame 2: RGB(255, 0, 0)
  - Frame 5, 7: RGB(75, 0, 0)
  - Frame 6: RGB(45, 0, 0)

### 4. 모서리 색상 패턴
```python
CORNER_COLORS = [
    [(246, 246, 255), (145, 145, 255)],  # Frame 0
    [(229, 229, 255), (208, 208, 255)],  # Frame 1
    [(153, 153, 255), (254, 254, 255)],  # Frame 2
    [(170, 170, 255), (191, 191, 255)],  # Frame 3
    [(246, 246, 255), (145, 145, 255)],  # Frame 4
    [(229, 229, 255), (208, 208, 255)],  # Frame 5
    [(153, 153, 255), (254, 254, 255)],  # Frame 6
    [(170, 170, 255), (191, 191, 255)],  # Frame 7
]
```

## 📝 구현 체크리스트

새 전설 아이템 제작 시:
- [ ] **초대형 단색 원형 배경 구현 (28픽셀 반경 고정)**
- [ ] **배경을 가장 먼저 그리기 (pygame.draw.circle 사용)**
- [ ] **배경 색상 8가지 프레임별 변화**
- [ ] 은색/보라색 모서리 장식 (3x3)
- [ ] 프레임별 변화하는 빨간 테두리
- [ ] 8개 프레임 모두 생성
- [ ] 아이템 고유 디자인은 중앙에만 배치
- [ ] PNG 파일 저장 (32x32 픽셀)

## 🔧 참조 코드
- 애니메이션 배경: `generate_hermes_detailed_boots.py` (배경 구현 참조)
- 전체 템플릿: `generate_legendary_template.py`

## ⚠️ 주의사항
- 절대 `generate_ragnarok_hammer_icon.py` 참조 금지 (잘못된 구버전)
- 테두리와 배경은 PNG 자체에 포함되어야 함 (런타임에 그리지 않음)
- 실제 PNG 파일 확인 필수
- **배경 원은 아이템 뒤에 배치 (그리는 순서: 배경 → 아이템 → 테두리)**